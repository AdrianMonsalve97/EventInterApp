import { CreateEventServiceTs } from './create-event.service.ts.ts';

describe('CreateEventServiceTs', () => {
  it('should create an instance', () => {
    expect(new CreateEventServiceTs()).toBeTruthy();
  });
});
