import { CreateEventValidatorTs } from './create-event.validator.ts.ts';

describe('CreateEventValidatorTs', () => {
  it('should create an instance', () => {
    expect(new CreateEventValidatorTs()).toBeTruthy();
  });
});
