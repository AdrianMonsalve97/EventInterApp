import { ListEventsComponentTs } from './list-events.component.ts';

describe('ListEventsComponentTs', () => {
  it('should create an instance', () => {
    expect(new ListEventsComponentTs()).toBeTruthy();
  });
});
